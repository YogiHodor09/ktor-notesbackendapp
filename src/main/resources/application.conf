ktor {
  development = true
  deployment {
    port = 8087
    port = ${?PORT}
  }
  application {
    modules = [com.ktrobackend.ApplicationKt.module]
  }
}
jwt {
  domain = "https://jwt-provider-domain/"
  audience = "jwt-audience"
  realm = "ktor sample app"
}
